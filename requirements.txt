# To create a new file: run pipreqs in the project root directory. 
# To update a requirements file to include current package versions: 
# run the pip freeze > requirements.txt in the project root directory.

Flask==3.1.1
flask_cors==6.0.1
openai==0.28.0
python-dotenv==1.1.1
